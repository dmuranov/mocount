// Dashboard placeholder — real table + volume input land in step 13c.

export default function Dashboard() {
  return (
    <div className="page">
      <h2>Dashboard</h2>
      <p className="mono">// table + volume input arrives in step 13c</p>
    </div>
  );
}
